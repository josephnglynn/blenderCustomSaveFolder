# blenderCustomSaveFolder

# usage
Open the Preferences ‣ Add-ons ‣ Install… and select the downloaded python file.

whola, a new rendering tab should appear and hey presto your renders will now same to %userprofile%/Documents/Blender Render Outputs/{project name}/{project Name + version}.{your set file type}
